# Fake secrets file for CI configuration validation.
# This file provides placeholder values so that hass --script check_config
# can run without the real secrets.yaml.
#
# Add any !secret references used in your config here with dummy values.
